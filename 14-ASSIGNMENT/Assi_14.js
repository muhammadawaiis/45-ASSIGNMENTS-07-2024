"use strict";
let invatation_card = ["Kinza", "Iqra", "Tooba"];
for (let i = 0; i < invatation_card.length; i++) {
    console.log(`Dear
    ! ${invatation_card[i]} , Would you like to go on Dinner with Me  to Night?  `);
    console.log(`Gusse the number of peoples invited : ${invatation_card}`);
}
